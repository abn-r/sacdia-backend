node_modules
dist
# Keep environment variables out of version control
.env
.claude
/generated/prisma
coverage/

# Runtime audit logs from one-shot scripts
orphan-cleanup-*.json

# Benchmark reports generated by scripts/benchmark-api.js
reports/benchmarks/

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/
